server:
  port: 8762
eureka:
  client:
    service-url:
      defaultZone: http://${other.eureka.host}:${other.eureka.port}/eureka
  instance:
    hostname: eureka1.tacocloud.com
other:
  eureka:
    host: localhost
    port: 8762
